---
status: stub
---

# Maps reference

::: warning Stub
Planned, not built — one page per registered `base_map` (bijection/morphism), a spec sheet (source, codomain,
`is_bijection`/`is_order_iso`, inverse, a live worked example) rather than the essay tour [the bijections
page](/learn/explorations/bijections) gives a handful of named ones. Real data to build it from already exists
(`base_map`, 22 `is_bijection` rows today) — scoping/build work, not a design question.
:::

Until this exists, [Bijections](/learn/explorations/bijections) tours the notable ones live, and [the API
reference's map section](/develop/api#representations-stats-maps-—-the-catalog-facets) lists every
registered map id.
