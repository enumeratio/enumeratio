<!-- GENERATED by docs/.vitepress/reference/generate.mts from nodes.ts — do not edit by hand. -->
# BellNumber

*The number of ways to partition an $n$-element set into non-empty, unordered blocks.*

## Usage

| Form | Meaning |
|---|---|
| $\operatorname{BellNumber}(n)$ | the $n$-th Bell number, $B_n$ |
| $\operatorname{BellNumber}(\{n_1, n_2, \dots\})$ | threads element-wise over a list (Listable) |

## Details

- **Arity:** 1 — a natural number $n$.
- **Result type:** `(integer) -> integer`, Listable — applied to a list, threads element-wise.
- **Implementation:** the Bell triangle (`packages/compute-engine/src/kernels-combinatorics.ts`) — each row built from the previous one in $O(n)$ additions, $O(n^2)$ total for $B_0, \dots, B_n$; not a closed form.
- **Domain:** $n \geq 0$; $B_0 = 1$ by convention (the empty set has exactly one partition: itself, with zero blocks).
- **Counts:** the collection `SetPartitions(n)` — $\operatorname{Count}(\operatorname{SetPartitions}(n)) = B_n$ by construction (`packages/compute-engine/src/packs/core.ts`).
- **Also known as:** Wolfram spells this `BellB`; both `BellB(n)` and the compute-engine-native `BellNumber(n)` resolve as aliases.

## Examples

$B_n$ counts the ways to partition $\{1, \dots, n\}$ into unordered blocks — enumerate `SetPartitions(3)`, and its count is $B_3$:

<ClientOnly>
  <enumeratio-expressions value='{"lines":[{"latex":"\\operatorname{SetPartitions}(3)"},{"latex":"\\left|\\operatorname{SetPartitions}(3)\\right|","expect":"5"}]}'></enumeratio-expressions>
</ClientOnly>

$B_6 = 203$:

<ClientOnly>
  <enumeratio-expressions value='{"lines":[{"latex":"\\operatorname{BellNumber}(6)","expect":"203"}]}'></enumeratio-expressions>
</ClientOnly>

## See also

`SetPartitions` · [`FubiniNumber`](/reference/FubiniNumber) (the ordered version) · [`PartitionNumber`](/reference/PartitionNumber) (integer, not set, partitions) · [`CatalanNumber`](/reference/CatalanNumber)
