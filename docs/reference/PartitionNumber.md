<!-- GENERATED by docs/.vitepress/reference/generate.mts from nodes.ts — do not edit by hand. -->
# PartitionNumber

*$p(n)$ — the number of integer partitions of $n$ (OEIS [A000041](https://oeis.org/A000041)).*

## Usage

| Form | Meaning |
|---|---|
| $\operatorname{PartitionNumber}(n)$ | $p(n)$ |
| $\operatorname{PartitionNumber}(\{n_1, n_2, \dots\})$ | threads element-wise over a list (Listable) |

## Details

- **Arity:** 1 — a natural number $n$.
- **Result type:** `(integer) -> integer`, Listable.
- **Implementation:** Euler's pentagonal-number recurrence, $p(n) = \sum_{k\geq1} (-1)^{k-1}\left(p\!\left(n - \tfrac{k(3k-1)}{2}\right) + p\!\left(n - \tfrac{k(3k+1)}{2}\right)\right)$, built bottom-up in one array (`packages/compute-engine/src/kernels-extra.ts`) — sub-quadratic (the pentagonal gaps grow, so each row's inner loop terminates well before $n$ terms).
- **Domain:** $n \geq 0$; a negative $n$ returns $0$.
- **Counts:** the collection [`IntegerPartitions`](/reference/IntegerPartitions) — $\operatorname{Count}(\operatorname{IntegerPartitions}(n)) = p(n)$ by construction.
- **Distinct from PartitionsQ:** `PartitionsQ(n)` counts partitions into *distinct* parts only — a different, smaller sequence with its own collection, `DistinctPartitions`.
- **Also known as:** Wolfram spells this `PartitionsP`; both `PartitionsP(n)` and the compute-engine-native `NPartition(n)` resolve as aliases.

## Examples

$p(5) = 7$:

<ClientOnly>
  <enumeratio-expressions value='{"lines":[{"latex":"\\operatorname{PartitionNumber}(5)","expect":"7"}]}'></enumeratio-expressions>
</ClientOnly>

It matches counting [`IntegerPartitions`](/reference/IntegerPartitions) directly:

<ClientOnly>
  <enumeratio-expressions value='{"lines":[{"latex":"\\left|\\operatorname{IntegerPartitions}(5)\\right|","expect":"7"}]}'></enumeratio-expressions>
</ClientOnly>

## See also

[`IntegerPartitions`](/reference/IntegerPartitions) · `PartitionsQ` · `DistinctPartitions` · [`BellNumber`](/reference/BellNumber) (set, not integer, partitions)
