<!-- GENERATED by docs/.vitepress/reference/generate.mts from nodes.ts — do not edit by hand. -->
# FubiniNumber

*The number of ways to partition an $n$-element set into non-empty blocks AND put those blocks in order — the ordered Bell numbers.*

## Usage

| Form | Meaning |
|---|---|
| $\operatorname{FubiniNumber}(n)$ | the $n$-th Fubini (ordered Bell) number |
| $\operatorname{FubiniNumber}(\{n_1, n_2, \dots\})$ | threads element-wise over a list (Listable) |

## Details

- **Arity:** 1 — a natural number $n$.
- **Result type:** `(integer) -> integer`, Listable.
- **Implementation:** the recurrence $\operatorname{FubiniNumber}(n) = \sum_{k=1}^{n} \binom{n}{k}\operatorname{FubiniNumber}(n-k)$, $\operatorname{FubiniNumber}(0) = 1$, built bottom-up in one array (`packages/compute-engine/src/kernels-combinatorics.ts`) — $O(n^2)$ total for the whole table up to $n$.
- **Domain:** $n \geq 0$.
- **Counts:** the collection `SetCompositions(n)` — ordered set partitions of $[n]$ (`packages/compute-engine/src/packs/core.ts`: `SetCompositions`'s count is literally the Fubini number).
- **Relation to BellNumber:** $\operatorname{FubiniNumber}(n) \geq B_n$ for $n \geq 1$ — every set partition contributes $k!$ ordered compositions, where $k$ is its number of blocks.
- **Notebook spelling:** the `FubiniNumber` symbol isn't application-parseable in the notebook yet (it has no compute-engine kernel head — unlike `BellNumber`/`PartitionNumber`); a live line counts `SetCompositions(n)` directly, which is the Fubini number by construction. Wiring the symbol is tracked in the compute-engine/catalog naming-alignment work.

## Examples

The 3 ordered set partitions of $\{1,2\}$ — enumerate `SetCompositions(2)`, whose count is the Fubini number $\operatorname{FubiniNumber}(2)$:

<ClientOnly>
  <enumeratio-expressions value='{"lines":[{"latex":"\\operatorname{SetCompositions}(2)"},{"latex":"\\left|\\operatorname{SetCompositions}(2)\\right|","expect":"3"}]}'></enumeratio-expressions>
</ClientOnly>

At $n = 4$, $\operatorname{FubiniNumber}(4) = 75$ — read off the collection count:

<ClientOnly>
  <enumeratio-expressions value='{"lines":[{"latex":"\\left|\\operatorname{SetCompositions}(4)\\right|","expect":"75"}]}'></enumeratio-expressions>
</ClientOnly>

## See also

`SetCompositions` · [`BellNumber`](/reference/BellNumber) (the unordered count) · [`CatalanNumber`](/reference/CatalanNumber)
