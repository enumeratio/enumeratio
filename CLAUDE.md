@AGENTS.md

<!-- The project instructions live in AGENTS.md (LLM-agnostic, committed), transcluded above. Personal,
     Claude-Code-specific, and local-setup notes live in a gitignored CLAUDE.local.md at the repo root, which
     Claude Code auto-loads (it walks up from the cwd, so worktree sessions pick up the primary checkout's copy).
     Keep this file to the transclusion only. -->
