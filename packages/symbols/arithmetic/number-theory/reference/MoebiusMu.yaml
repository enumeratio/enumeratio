name: MoebiusMu
domain: Number theory
signature: MoebiusMu(n)
summary: "The Möbius function: 0 if n has a squared prime factor, else (-1)^(number of prime factors)."
signatures:
  - call: MoebiusMu(n)
    description: the Möbius function $\mu(n)$.
details:
  - $\mu(n)=0$ if $n$ has a squared prime factor; otherwise $\mu(n)=(-1)^{\omega(n)}$ for $\omega(n)$ distinct prime factors.
  - $\mu(1)=1$ by convention, the empty product.
  - 'Underlies Möbius inversion: $\sum_{d\mid n}\mu(d)=0$ for every $n>1$.'
  - $n$ is squarefree exactly when $\mu(n)\neq0$. See [[IsSquareFree]].
  - $\mu$ is only defined on positive integers; compute-engine leaves $n\le0$ unevaluated.
seeAlso:
  - FactorInteger
  - IsSquareFree
references:
  - system: wikipedia
    identity: Möbius function
  - system: mathworld
    identity: MoebiusFunction
  - system: dlmf
    identity: "27.2"
  - system: oeis
    identity: A008683
names:
  wolframIdentity: true
bindings:
  - origin: mapped
    form: wolfram
    template: MoebiusMu[$1]
    arity: 1
    threadArg: 1
  - origin: mapped
    form: sympy
    template: mobius($1)
    arity: 1
    threadArg: 1
  - origin: mapped
    form: sage
    template: moebius($1)
    arity: 1
    threadArg: 1
  - origin: mapped
    form: oscar
    template: moebius_mu($1)
    arity: 1
    threadArg: 1
  - origin: mapped
    form: julia
    template: moebius_mu($1)
    arity: 1
    threadArg: 1
  - origin: mapped
    form: mathlib4
    template: (ArithmeticFunction.moebius $1)
    arity: 1
    threadArg: 1
