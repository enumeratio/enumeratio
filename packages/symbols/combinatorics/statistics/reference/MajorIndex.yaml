name: MajorIndex
domain: Permutations statistics
signature: MajorIndex(p)
summary: The SUM of the descent positions — not their count.
details:
  - Defined over `Permutations` as an expression in `_x`, evaluated by compute-engine — the definition IS the implementation.
  - "Takes a `Permutations`, and also a bare list of integers: this reading compares entries with each other rather than with their positions, so it stands on any sequence."
  - The distinction from Descents is the entire content of the statistic.
  - "A separate definition exists for `DyckPaths` (The sum of the descent positions of the step word — where an up step is followed by a down step.) but is not the one declared: one head, one owner."
