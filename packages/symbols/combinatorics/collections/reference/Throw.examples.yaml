- id: throw-aborts-to-the-nearest-catch
  expr: [Catch, [Throw, abandoned]]
  expected: abandoned
  caption: Throw aborts evaluation, Catch recovers the value
- id: a-tag-mismatch-propagates-past-a-catch
  expr: [Catch, [Catch, [Throw, deep, x], y], x]
  expected: deep
  caption: a tag mismatch propagates past a non-matching Catch to one that matches
  category: Scope
