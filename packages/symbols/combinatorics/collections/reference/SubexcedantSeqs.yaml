name: SubexcedantSeqs
domain: Collections
signature: SubexcedantSeqs(n)
summary: 'Subexcedant sequences of length $n$: words $(a_0, …, a_{n-1})$ with $0 \le a_i \le i$ — another factorial-base encoding of permutations.'
signatures:
  - call: SubexcedantSeqs(n)
    description: 'Subexcedant sequences of length $n$: words $(a_0, …, a_{n-1})$ with $0 \le a_i \le i$ — another factorial-base encoding of permutations'
    library: enumeratio-collections
details:
  - Count is $n!$ (A000142); a sibling of [[LehmerCodes]] with the bound $a_i \le i$ instead of $a_i \le n-1-i$.
seeAlso:
  - SymmetricGroup
  - LehmerCodes
references:
  - system: oeis
    identity: A000142
catalog:
  - system: oeis
    identity: A000142
    url: https://oeis.org/A000142
    note: cardinality only; distinct word-representation
