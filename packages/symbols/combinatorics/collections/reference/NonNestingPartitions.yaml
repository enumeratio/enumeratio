name: NonNestingPartitions
domain: Collections
signature: NonNestingPartitions(n)
summary: The partitions of $\{1, …, n\}$ with no two blocks whose consecutive-element arcs nest.
signatures:
  - call: NonNestingPartitions(n)
    description: the partitions of $\{1, …, n\}$ with no two blocks whose consecutive-element arcs nest
    library: enumeratio-collections
details:
  - Count is the Catalan number $C_n$ (A000108).
seeAlso:
  - SetPartitions
  - NonCrossingPartitions
  - DyckPaths
references:
  - system: oeis
    identity: A000108
catalog:
  - system: oeis
    identity: A000108
    url: https://oeis.org/A000108
    note: cardinality only
  - system: sage
    identity: sage.combinat.set_partition.SetPartition.is_nonnesting
    url: https://doc.sagemath.org/html/en/reference/combinat/sage/combinat/set_partition.html
    note: predicate method, not a dedicated class — filter SetPartitions(n) by .is_nonnesting()
    relation: partial
grades:
  - name: n
    role: axis
carrier: SetPartition
