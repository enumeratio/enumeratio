{
  "name": "@enumeratio/algebra",
  "version": "0.0.0",
  "private": true,
  "description": "The shared algebra seam for compute-engine extensions: one set of heads (Basis, AlgebraDimension, the ordered product, Element) dispatching over registered providers, so several algebra libraries can coexist on one engine.",
  "license": "WTFPL",
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs"
    },
    "./src": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "vp pack",
    "dev": "vp pack --watch",
    "test": "vp test",
    "check": "vp check"
  },
  "devDependencies": {
    "@cortex-js/compute-engine": "^0.134.0",
    "typescript": "^7.0.2",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "peerDependencies": {
    "@cortex-js/compute-engine": "^0.134.0"
  }
}
