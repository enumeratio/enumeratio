name: PermutationGroup
domain: Permutations
signature: PermutationGroup({perm, ...})
summary: The group generated by a list of permutations ([[Cycles]]), closed by breadth-first search over products of the generators. A carrier, like [[Cycles]] — [[GroupOrder]], [[GroupElements]] and [[GroupGenerators]] read it directly rather than it computing anything itself.
signatures:
  - call: PermutationGroup({perm, ...})
    description: the group $\langle \mathrm{perm}, \dots \rangle$ generated by the given permutations
    library: enumeratio-groupalgebra
  - call: PermutationGroup({perm, ...}, n)
    description: the same group, with its degree (largest point moved) at least `n`
    library: enumeratio-groupalgebra
details:
  - An empty generator list is the trivial group, order 1
  - A single generator gives the cyclic group of its order — the least common multiple of its cycle lengths
  - Dihedral groups arise as `PermutationGroup` of an $n$-cycle (rotation) and a 2-cycle-full involution (a flip)
examples:
  - id: a-cyclic-group
    expr: [GroupOrder, [PermutationGroup, [List, [Cycles, [List, [List, 1, 9, 6], [List, 3, 7]]]]]]
    expected: 6
    caption: lcm(3, 2) — one 3-cycle and one 2-cycle in the single generator
  - id: s4-from-two-generators
    expr:
      [
        GroupOrder,
        [
          PermutationGroup,
          [List, [Cycles, [List, [List, 1, 2, 3, 4]]], [Cycles, [List, [List, 1, 2]]]],
        ],
      ]
    expected: 24
    caption: a 4-cycle and a transposition generate all of $S_4$
    category: Applications
  - id: the-dihedral-group-of-the-square
    expr:
      [
        GroupOrder,
        [
          PermutationGroup,
          [List, [Cycles, [List, [List, 1, 2, 3, 4]]], [Cycles, [List, [List, 1, 3]]]],
        ],
      ]
    expected: 8
    caption: rotation + a flip generate the dihedral group of order 8
    category: Applications
seeAlso:
  - Cycles
  - GroupOrder
  - GroupElements
  - GroupGenerators
  - DihedralGroup
  - CyclicGroup
