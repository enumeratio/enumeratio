-- requires-tag: collection
-- paths half of sqlsrc/fiber_unrank_verify.sql (#283 phase 3 extraction) — one row per this pack's own indexable
-- collection, split out because these rows call the pack's own collection constructors directly (element_at over
-- fibers(<paths-collection>(...))), which don't exist loading core alone.
INSERT INTO base_example (suite, title, kind, expected, description, sql) VALUES
('fiber_unrank','colored_motzkin_paths: element_at == iterator','eq','true','direct unrank agrees with the floor (#286)',$q$ SELECT coalesce(bool_and((element_at(f, ord)).value = (SELECT (e).value FROM elements(f, ord+1) e ORDER BY e OFFSET ord LIMIT 1)), true)::text FROM fibers(colored_motzkin_paths(5,2)) f, generate_series(0, 8) ord WHERE ord < cardinality(f) $q$),
('fiber_unrank','delannoy_paths: element_at == iterator','eq','true','direct unrank agrees with the floor (#286)',$q$ SELECT coalesce(bool_and((element_at(f, r)).value = (SELECT (e).value FROM elements(f, r+1) e ORDER BY e OFFSET r LIMIT 1)), true)::text FROM fibers(delannoy_paths(3)) f, generate_series(0, 8) r WHERE r < cardinality(f) $q$),
('fiber_unrank','dyck_paths_by_height: element_at == iterator','eq','true','direct unrank agrees with the floor (#286)',$q$ SELECT coalesce(bool_and((element_at(f, r)).value = (SELECT (e).value FROM elements(f, r+1) e ORDER BY e OFFSET r LIMIT 1)), true)::text FROM fibers(dyck_paths_by_height(6,3)) f, generate_series(0, 8) r WHERE r < cardinality(f) $q$),
('fiber_unrank','fine_paths: element_at == iterator','eq','true','direct unrank agrees with the floor (#286)',$q$ SELECT coalesce(bool_and((element_at(f, r)).value = (SELECT (e).value FROM elements(f, r+1) e ORDER BY e OFFSET r LIMIT 1)), true)::text FROM fibers(fine_paths(6)) f, generate_series(0, 8) r WHERE r < cardinality(f) $q$),
('fiber_unrank','grand_dyck_paths: element_at == iterator','eq','true','direct unrank agrees with the floor (#286)',$q$ SELECT coalesce(bool_and((element_at(f, r)).value = (SELECT (e).value FROM elements(f, r+1) e ORDER BY e OFFSET r LIMIT 1)), true)::text FROM fibers(grand_dyck_paths(4)) f, generate_series(0, 8) r WHERE r < cardinality(f) $q$),
('fiber_unrank','k_motzkin_paths: element_at == iterator','eq','true','direct unrank agrees with the floor (#286)',$q$ SELECT coalesce(bool_and((element_at(f, r)).value = (SELECT (e).value FROM elements(f, r+1) e ORDER BY e OFFSET r LIMIT 1)), true)::text FROM fibers(k_motzkin_paths(6,2)) f, generate_series(0, 8) r WHERE r < cardinality(f) $q$),
('fiber_unrank','motzkin_paths: element_at == iterator','eq','true','direct unrank agrees with the floor (#286)',$q$ SELECT coalesce(bool_and((element_at(f, r)).value = (SELECT (e).value FROM elements(f, r+1) e ORDER BY e OFFSET r LIMIT 1)), true)::text FROM fibers(motzkin_paths(6)) f, generate_series(0, 8) r WHERE r < cardinality(f) $q$),
('fiber_unrank','motzkin_paths_by_peaks: element_at == iterator','eq','true','direct unrank agrees with the floor (#286)',$q$ SELECT coalesce(bool_and((element_at(f, r)).value = (SELECT (e).value FROM elements(f, r+1) e ORDER BY e OFFSET r LIMIT 1)), true)::text FROM fibers(motzkin_paths_by_peaks(5,1)) f, generate_series(0, 8) r WHERE r < cardinality(f) $q$);
