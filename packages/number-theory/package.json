{
  "name": "@enumeratio/number-theory",
  "version": "0.0.0",
  "private": true,
  "description": "Arithmetic in Z/m for @cortex-js/compute-engine: every r-th root of a residue (PowerModList), discrete logarithms, primitive roots and rational reconstruction, all over bigints.",
  "license": "WTFPL",
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs"
    },
    "./src": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "vp pack",
    "dev": "vp pack --watch",
    "test": "vp test",
    "check": "vp check"
  },
  "dependencies": {
    "@enumeratio/boxed": "workspace:*"
  },
  "devDependencies": {
    "@cortex-js/compute-engine": "^0.128.0",
    "@enumeratio/boxed": "workspace:*",
    "typescript": "^7.0.2",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "peerDependencies": {
    "@cortex-js/compute-engine": "^0.128.0"
  }
}
