{
  "name": "@enumeratio/boxed",
  "version": "0.0.0",
  "private": true,
  "description": "Value extraction from @cortex-js/compute-engine BoxedExpressions: operands, integers and strings, read through checked accessors instead of casts.",
  "license": "WTFPL",
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs"
    },
    "./src": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "vp pack",
    "dev": "vp pack --watch",
    "test": "vp test",
    "check": "vp check"
  },
  "devDependencies": {
    "@cortex-js/compute-engine": "^0.128.0",
    "typescript": "^7.0.2",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "peerDependencies": {
    "@cortex-js/compute-engine": "^0.128.0"
  }
}
