{
  "name": "@enumeratio/bench",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-system benchmarks: a catalogue of cases as data, native scripts generated per system through the oracle's emit, one measurement protocol, and per-run JSON reports. See design/benchmarking.md.",
  "license": "WTFPL",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "vp test",
    "check": "vp check",
    "bench": "node scripts/bench.ts"
  },
  "dependencies": {
    "@enumeratio/entry": "workspace:*",
    "@enumeratio/oracle": "workspace:*"
  },
  "devDependencies": {
    "@cortex-js/compute-engine": "^0.134.0",
    "@types/node": "catalog:",
    "typescript": "^7.0.2",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  }
}
