name: Factorial
domain: Combinatorics
signature: Factorial(n)
summary: The product of the positive integers up to n, written n!.
signatures:
  - call: Factorial(n)
    description: $n!$, the product of the integers from 1 to n.
details:
  - $0! = 1$ by convention, the empty product.
  - 'Recurrence: $n! = n\,(n-1)!$.'
  - Extends to non-integers via the Gamma function, $n! = \Gamma(n+1)$, so half-integer factorials involve $\sqrt{\pi}$.
  - Grows faster than any exponential; Stirling's approximation $n! \sim \sqrt{2\pi n}\,(n/e)^n$ describes the asymptotics.
  - compute-engine returns ComplexInfinity at negative integers, the poles of Gamma, rather than leaving the expression unevaluated.
seeAlso:
  - Binomial
  - Factorial2
  - Subfactorial
  - Pochhammer
references:
  - system: wikipedia
    identity: Factorial
  - system: mathworld
    identity: Factorial
  - system: dlmf
    identity: "5.2"
  - system: rosettacode
    identity: Factorial
names:
  wolframIdentity: true
bindings:
  - origin: mapped
    form: wolfram
    template: Factorial[$1]
    arity: 1
  - origin: mapped
    form: sympy
    template: factorial($1)
    arity: 1
  - origin: mapped
    form: sage
    template: factorial($1)
    arity: 1
  - origin: mapped
    form: oscar
    template: factorial(ZZ($1))
    arity: 1
  - origin: mapped
    form: julia
    template: factorial(ZZ($1))
    arity: 1
  - origin: mapped
    form: mathlib4
    template: (Nat.factorial $1)
    arity: 1
  - origin: mapped
    form: rust
    template: factorial($1)
    arity: 1
catalog:
  - system: mathlib4
    identity: Nat.factorial
    url: https://leanprover-community.github.io/mathlib4_docs/Mathlib/Data/Nat/Factorial/Basic.html
  - system: wolfram
    identity: Factorial
    url: https://reference.wolfram.com/language/ref/Factorial.html
