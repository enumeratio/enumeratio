// The entry types live in `@enumeratio/entry` (a leaf, so the packages whose heads these
// entries document can type theirs without depending on this package). Re-exported here so
// the rest of `reference` keeps its short import.
export type * from "@enumeratio/entry";
