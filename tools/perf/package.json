{
  "name": "@enumeratio/perf-tools",
  "version": "0.0.0",
  "private": true,
  "description": "Advisory perf-drift tooling for CI: runs each package's suite with the vitest JSON reporter, keeps a rolling history, and flags tests that drift past their trailing median. See tools/perf/README.md.",
  "license": "WTFPL",
  "type": "module",
  "scripts": {
    "test": "vp test",
    "check": "vp check",
    "collect": "node src/collect.ts",
    "cpu-prof": "node src/cpu-prof.ts"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "^7.0.2",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  }
}
